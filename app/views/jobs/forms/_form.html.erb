<%= form_with model: job do |f| %>

    <%= render "jobs/forms/shared_fields", job: job, f: f %>

    <%= render "jobs/forms/location_fields", locations: @locations, f: f %>

    <div class="actions">
        <%= f.submit "Post Job", class: "btn btn-outline-primary" %>
    </div>

<% end %>