<div class="container-fluid" style="width: 50%">
    <h1>All Locations</h1>
    <br>
    <div>
        <ol>
            <% @locations.each do |location| %>
                <li><%= link_to "#{location.city}", location_jobs_url(location) %></li>
            <% end %>
        </ol>
    </div>
</div>